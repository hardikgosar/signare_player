class AppConstants {
  static const assetsPath = 'assets/content.json';
  static const normalContentDuration = Duration(seconds: 10);
  static const customImageDuration = Duration(seconds: 5);

  const AppConstants._();
}